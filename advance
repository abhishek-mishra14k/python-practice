if(n := len([1,2,3,4,5])) >3:
    print(f"list is too long ({n} elements expected are <=3)")


# Variable type hint
age: int = 25
# Function type hints
def greeting(name: str) -> str:
    return f"Hello, {name}!"

# Usage
print(greeting("Alice")) # Output: Hello, Alice!


def http_status(status):
    match status:
        case 200:
           return "OK"
        case 404:
            return "Not Found"
        case 500:
            return "Internal Server Error"
        case _:
           return "Unknown status"
# Usage
print(http_status(200)) # Output: OK
print(http_status(404)) # Output: Not Found
print(http_status(500)) # Output: Internal Server Error
print(http_status(403)) # Output: Unknown status


dict1 = {'a': 1, 'b': 2}
dict2 = {'b': 3, 'c': 4}
merged = dict1 | dict2
print(merged) # Output: {'a': 1, 'b': 3, 'c': 4}


# sometimes we want to take the input from user as an int but by mistake if he types the str in it so programme returns an error so to avoid that and tell user to enter the correct value we use this

try :
    a = int(input("hey enter a number : "))
    print(a)

except ValueError as v:
    print("hry")
    print(v)

except Exception as e:
    print(e)


print("thank you !")

# this avoids the crashing of programme and continue the programme


a = int(input("enter the first number :"))
b = int(input("enter the second nmber :"))


if( b==0):
    raise ZeroDivisionError ("our programme is not meant to divide the number by zero ")  #this will not let the user move AHEAD IN THE PROGRAMME 

else:
   print(f"a divide by b is : {a/b}")



try :
    a = int(input("hey enter a number : "))
    print(a)


except Exception as e:
    print(e)

else:
    print("thank you ! i am inside else ")  #programme goes inside the else only when the try block runs successfully 



# in function when we return it means the code below the return will not run but in this try and finally sitaution the finally block override the code returns the value that is inside teh finally block means it will run at all the conditons 


def main():
    try :
        a = int(input("hey enter a number : "))
        print(a)


    except Exception as e:
        print(e)

    
        print("thank you ! i am inside finally ")

main()



a = 45  #this is a global variable 

def fun():
    global a  #this will change the global varibales value 
    a = 3  #and this is a local varibale 
    print(a)

fun()
print (a)




l = [1,3,5,7,89]

# index = 0
# for item in l :
#     print(f"the item number at index {index} is {item}")
#     index += 1

                # this can be done by using enumerate function 

for index, item in enumerate(l):
    print(f"the item number at index {index} is {item}")




#   this name function tells the name of the file it running like if u run this here it returns the MAIN and u run this in main file then itreurn the module where it is written

def myfunc():
    print("hellowrold  34")


myfunc()

if __name__ == "__main__":  
    #IF THIS CODE IS DIRCTLY EXECUTED BY THE RUNNING THE FILE IT IS PRESENT 
    print("we are directly running this code ! ")
    myfunc()
    print(__name__) 




